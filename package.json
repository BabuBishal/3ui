{
  "name": "l3ui-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "app/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -C app/docs dev",
    "lint": "pnpm -r lint",
    "format": "pnpm -r format"
  },
  "pnpm": {
    "overrides": {
      "react": "^19.0.0",
      "react-dom": "^19.0.0"
    }
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "vite-plugin-static-copy": "^3.1.4"
  }
}
